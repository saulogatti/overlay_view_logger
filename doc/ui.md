## UI

Camada de apresentação do app e a integração com o `OverlayEntry`.

Aqui entram widgets e componentes que o usuário final enxerga, como a tela principal (`MainApp`) e a visão do overlay (`OverlayAppView` e `OverlayAppViewManager`).

