## Controllers

Camada responsável por orquestrar casos de uso entre a UI e o repositório.

Neste projeto, o principal componente é o `OverlayBloc`, que emite estados de loading/success/error e dispara consultas no `RegisterRepository`.

