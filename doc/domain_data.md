## Domínio e Dados

Entidades e contratos do domínio, além das implementações que persistem e consultam dados.

Aqui ficam:
- `RegisterEntity` (entidade do registro)
- `RegisterRepository` e sua implementação (`RegisterRepositoryImpl`)
- `TypeRegister` (tipos de registro)

